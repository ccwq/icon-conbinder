# AGENTS

## Agent skills

### Issue tracker

这个仓库的 issues 和 PRD 以 Markdown 文件形式保存在 `.scratch/` 下。见 `docs/agents/issue-tracker.md`。

### Triage labels

triage 角色映射使用本仓库的默认标签字符串。见 `docs/agents/triage-labels.md`。

### Domain docs

这个仓库采用单上下文布局。见 `docs/agents/domain.md`。
